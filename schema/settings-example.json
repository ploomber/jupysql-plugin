{
    "title": "JupySQL",
    "description": "JupySQL settings.",
    "properties": {
        "showFormatSQL": {
            "type": "boolean",
            "title": "Show Format SQL button",
            "description": "If set, a button will be shown in the toolbar to format the SQL code.",
            "default": true
        },
        "showDeployNotebook": {
            "type": "boolean",
            "title": "Show Deploy Notebook button",
            "description": "If set, a button will be shown in the toolbar to deploy the notebook to Ploomber Cloud.",
            "default": true
        }
    },
    "additionalProperties": false,
    "type": "object"
}