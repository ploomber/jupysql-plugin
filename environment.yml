# development environment for jupysql-plugin
name: jupysql-plugin

channels:
  - conda-forge

dependencies:
  - python=3.11
  - nodejs=16
  - jupyterlab<4 # this will install jlpm
  - pip
  - pip:
    - build
    - twine
    - hatch
    - jupysql