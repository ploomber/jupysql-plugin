# development environment for jupysql-plugin
name: jupysql-plugin

channels:
  - conda-forge

dependencies:
  - python=3.11
  - nodejs=16.19.0=h7f372e8_1
  - jupyterlab<=4 # this will install jlpm
  - pip
  - pip:
    - build
    - twine
    - hatch
    - jupysql