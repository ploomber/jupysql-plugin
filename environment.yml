# development environment for jupysql-plugin
name: jupysql-plugin

channels:
  - conda-forge

dependencies:
  - python=3.11
  - nodejs>17
  - jupyterlab<4 # this will install jlpm
  - pip
  - pip:
    - build
    - twine
    - hatch>=1.5.0
    - jupysql